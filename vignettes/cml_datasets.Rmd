---
title: "CML datasets"
---

```{r, include = FALSE}
knitr::opts_chunk$set(
    collapse = TRUE,
    comment = "#>",
    warning = FALSE,
    message = FALSE
)
```

**Include some background info on these samples, treatments, experimental design**

```{r setup}
library(haemdata)
library(magrittr)
library(SummarizedExperiment)

use_pinboard("onedrive")

cml_mice <- get_pin("mmu_mrna_all_mice_GENCODEm28_HLT_qc.rds") %>%
    subset(select = grepl("CML", SummarizedExperiment::colData(.)$project))

pca <- pca_se(cml_mice)
```

```{r pca_plots}
pca$biplot
pca$pairs_plot
```
```{r pca_corr, fig.height=9, fig.width=8}
pca$correlation_plot
```

```{r transgenes}
plot_transgenes_se(cml_mice)
```
